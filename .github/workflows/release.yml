name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  source-release:
    name: Source archive, binary, and provenance
    if: github.ref_type == 'tag'
    runs-on: macos-15
    timeout-minutes: 30
    permissions:
      attestations: write
      checks: read
      contents: write
      id-token: write
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit
      - name: Check out tagged source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Require an annotated tag on main
        shell: bash
        run: |
          tag_ref="refs/tags/$GITHUB_REF_NAME"
          if ! git show-ref --verify --quiet "$tag_ref"; then
            printf 'error: checked-out tag is missing: %s\n' \
              "$GITHUB_REF_NAME" >&2
            exit 1
          fi
          if [[ "$(git cat-file -t "$tag_ref")" != tag ]]; then
            printf 'error: releases require an annotated tag; %s is not annotated\n' \
              "$GITHUB_REF_NAME" >&2
            exit 1
          fi
          tagged_commit="$(git rev-parse "$tag_ref^{commit}")"
          if [[ "$tagged_commit" != "$GITHUB_SHA" ]]; then
            printf 'error: annotated tag %s resolves to %s, expected %s\n' \
              "$GITHUB_REF_NAME" "$tagged_commit" "$GITHUB_SHA" >&2
            exit 1
          fi

          git fetch --no-tags origin \
            '+refs/heads/main:refs/remotes/origin/main'
          if ! git merge-base --is-ancestor \
            "$GITHUB_SHA" refs/remotes/origin/main; then
            printf 'error: tagged commit %s is not on main\n' \
              "$GITHUB_SHA" >&2
            exit 1
          fi

          tag_object="$(git rev-parse "$tag_ref")"
          tag_record="$(git ls-remote --exit-code origin "$tag_ref")"
          if [[ "$tag_record" != "$tag_object"$'\t'"$tag_ref" ]]; then
            printf 'error: remote annotated tag does not match the checked-out tag object\n' >&2
            exit 1
          fi
          peeled_ref="$tag_ref^{}"
          peeled_record="$(git ls-remote --exit-code origin "$peeled_ref")"
          if [[ "$peeled_record" != "$GITHUB_SHA"$'\t'"$peeled_ref" ]]; then
            printf 'error: remote annotated tag does not resolve to the tagged commit\n' >&2
            exit 1
          fi
      - name: Require successful CI for the tagged commit
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          check_runs="$(gh api \
            -H 'Accept: application/vnd.github+json' \
            "repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA/check-runs?per_page=100")"
          for required_check in \
            'Workflows and API contract' \
            'Build, lint, analyze, and test' \
            'Install layout' \
            'Analyze GitHub Actions' \
            'Gitleaks' \
            'Supply-chain analysis'; do
            jq -e --arg name "$required_check" '
              any(.check_runs[];
                .name == $name and
                .status == "completed" and
                .conclusion == "success")
            ' <<< "$check_runs" >/dev/null || {
              printf 'Required check is not successful: %s\n' \
                "$required_check" >&2
              exit 1
            }
          done
      - name: Validate release version
        id: metadata
        shell: bash
        run: |
          if [[ ! "$GITHUB_REF_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            printf 'error: release tag must have the form vMAJOR.MINOR.PATCH\n' >&2
            exit 1
          fi
          version="${GITHUB_REF_NAME#v}"
          project_version="$(< VERSION)"
          if [[ "$version" != "$project_version" ]]; then
            printf 'error: tag version %s does not match VERSION (%s)\n' \
              "$version" "$project_version" >&2
            exit 1
          fi
          printf 'version=%s\n' "$version" >> "$GITHUB_OUTPUT"
      - name: Create source archive and checksum
        env:
          VERSION: ${{ steps.metadata.outputs.version }}
        run: |
          set -o pipefail
          mkdir -p dist
          export COPYFILE_DISABLE=1
          git archive --format=tar --prefix="imessage-proxy-$VERSION/" "$GITHUB_SHA" |
            gzip -n -9 > "dist/imessage-proxy-$VERSION.tar.gz"
          test "$(tar -xOzf "dist/imessage-proxy-$VERSION.tar.gz" \
            "imessage-proxy-$VERSION/REVISION")" = "$GITHUB_SHA"
      # Built from the tagged tree that was just archived, on the same runner, so
      # the binary and the source in this release are the same commit by
      # construction rather than by convention. The recipe lives in the Makefile
      # as `release-binary` and CI runs the same target on every pull request, so
      # this path is exercised before a tag exists rather than at tag time.
      #
      # It carries no Developer ID signature and is not notarized - this
      # repository holds no signing identity - only the ad-hoc signature that
      # Apple silicon requires to execute at all. What that costs an operator is
      # in docs/install.md.
      - name: Build the universal server binary
        env:
          VERSION: ${{ steps.metadata.outputs.version }}
        run: |
          make release-binary STRICT=1
          cp build/universal/imessage-proxy-server \
            "dist/imessage-proxy-server-$VERSION-macos-universal"
      # One step writes the whole file rather than each artifact appending its
      # own line, so a re-run cannot leave a SHA256SUMS with an entry twice or a
      # partial list if an earlier step is skipped.
      - name: Checksum the release artifacts
        env:
          VERSION: ${{ steps.metadata.outputs.version }}
        run: |
          cd dist
          shasum -a 256 \
            "imessage-proxy-$VERSION.tar.gz" \
            "imessage-proxy-server-$VERSION-macos-universal" > SHA256SUMS
          shasum -a 256 --check SHA256SUMS
      - name: Attest the release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            dist/imessage-proxy-${{ steps.metadata.outputs.version }}.tar.gz
            dist/imessage-proxy-server-${{ steps.metadata.outputs.version }}-macos-universal
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          verify_remote_tag() {
            local peeled_record peeled_ref tag_object tag_record tag_ref
            tag_ref="refs/tags/$GITHUB_REF_NAME"
            if [[ "$(git cat-file -t "$tag_ref")" != tag ]]; then
              printf 'error: release tag is no longer an annotated tag\n' >&2
              return 1
            fi
            tag_object="$(git rev-parse "$tag_ref")"
            tag_record="$(git ls-remote --exit-code origin "$tag_ref")"
            if [[ "$tag_record" != "$tag_object"$'\t'"$tag_ref" ]]; then
              printf 'error: remote annotated tag changed during release\n' >&2
              return 1
            fi
            peeled_ref="$tag_ref^{}"
            peeled_record="$(git ls-remote --exit-code origin "$peeled_ref")"
            if [[ "$peeled_record" != "$GITHUB_SHA"$'\t'"$peeled_ref" ]]; then
              printf 'error: remote annotated tag no longer resolves to the tagged commit\n' >&2
              return 1
            fi
          }
          verify_remote_tag
          archive="dist/imessage-proxy-${{ steps.metadata.outputs.version }}.tar.gz"
          archive_name="${archive##*/}"
          binary="dist/imessage-proxy-server-${{ steps.metadata.outputs.version }}-macos-universal"
          binary_name="${binary##*/}"
          if release_is_draft="$(gh release view "$GITHUB_REF_NAME" --json isDraft --jq .isDraft 2>/dev/null)"; then
            if [[ "$release_is_draft" != true ]]; then
              temporary="$(mktemp -d)"
              trap 'rm -rf -- "$temporary"' EXIT
              # Every asset is compared, not just the archive. An asset left out
              # of this list would let a published release that is missing it, or
              # carries a stale copy of it, report itself as already matching.
              gh release download "$GITHUB_REF_NAME" \
                --dir "$temporary" \
                --pattern SHA256SUMS \
                --pattern "$archive_name" \
                --pattern "$binary_name"
              cmp -s dist/SHA256SUMS "$temporary/SHA256SUMS"
              cmp -s "$archive" "$temporary/$archive_name"
              cmp -s "$binary" "$temporary/$binary_name"
              verify_remote_tag
              printf 'Published release assets already match this tag; no mutation required.\n'
              exit 0
            fi
          else
            gh release create "$GITHUB_REF_NAME" \
              --draft \
              --generate-notes \
              --title "iMessage Proxy $GITHUB_REF_NAME" \
              --verify-tag
          fi
          gh release upload "$GITHUB_REF_NAME" dist/SHA256SUMS "$archive" "$binary" --clobber
          verify_remote_tag
          gh release edit "$GITHUB_REF_NAME" --draft=false
